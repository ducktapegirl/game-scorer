Round radio button — teal dot when selected. Use in small choice groups (e.g. scoring mode: "Highest wins" / "Lowest wins").

```jsx
<Radio label="Highest score wins" checked={mode === 'high'} onChange={() => setMode('high')} />
```
