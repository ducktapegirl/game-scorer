Styled dropdown for pick-one lists (e.g. choosing a game from the library, choosing round count).

```jsx
<Select label="Game" options={[{value:'wingspan', label:'Wingspan'}]} value={game} onChange={setGame} />
```
