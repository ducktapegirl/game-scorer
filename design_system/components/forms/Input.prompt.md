Labeled text input, used for player names, custom game names, score entry (numeric).

```jsx
<Input label="Player name" value={name} onChange={setName} placeholder="e.g. Priya" />
```

Focus state adds a teal ring + border; `error` prop switches border/helper text to danger red.
