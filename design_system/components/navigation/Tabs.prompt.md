Pill-track segmented control — used for switching between e.g. "Active games" / "History", or "Scoreboard" / "Rounds".

```jsx
<Tabs tabs={[{value:'active',label:'Active'},{value:'history',label:'History'}]} value={tab} onChange={setTab} />
```
