Circular icon-only button for compact actions.

```jsx
<IconButton name="trash-2" variant="ghost" onClick={() => {}} />
```

Variants: `ghost` (default, transparent) and `filled` (coral, for emphasis). Sizes `sm`/`md`/`lg` (32/40/48px — `md`/`lg` meet the 44px min tap target).
