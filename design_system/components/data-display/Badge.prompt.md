Small status pill for compact state labels.

```jsx
<Badge tone="success">Winning</Badge>
```

Tones: `neutral`, `primary`, `secondary`, `success`, `warning`, `danger` — each a soft-tint background with matching dark-tint text (never the raw saturated brand color as text).
